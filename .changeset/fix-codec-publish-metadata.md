---
"@sylphx/codec": patch
"@sylphx/codec-cli": patch
---

Forward-fix published package dependency metadata so internal workspace dependencies are materialized from the current workspace versions during npm publication.
