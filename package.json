{
	"name": "codec",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "bun run --filter '@sylphx/codec-core' build && bun run --filter '@sylphx/codec' build && bun run --filter '@sylphx/codec-cli' build",
		"test": "bun run --filter '@sylphx/*' test",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"changeset": "changeset",
		"version": "changeset version",
		"release": "bun run build && echo \"Direct changeset publish is unsafe for Bun workspaces; use the Sylphx release workflow.\" && exit 1"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@changesets/cli": "^2.27.10"
	}
}
